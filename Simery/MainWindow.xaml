<Window x:Class="Simery.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Simery"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400" Background="#FFFDFAF1">

    
     

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" Text="Список дел" FontSize="26" HorizontalAlignment="Center" />

        <ScrollViewer Grid.ColumnSpan="2" Grid.Row="1" Grid.Column="0" Margin="5" Background="#FDF5DF" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ListBox  Margin="5" Name ="ListToDo" Background="#FDF5DF">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Path = CaseName}"></TextBlock>
                            <TextBlock Text="{Binding Path = TimeOfCompleating, StringFormat=dd.MM.yyyy}"></TextBlock>
                            <TextBlock Text="{Binding Path = Description}" ></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>
        </ScrollViewer>

        


        

        <Button Grid.Column="0" Grid.Row="2" Name="buttonDel" Click="DelCase" Content="Удалить дело" Background="#5EBEC4" Height="30" Width="100" Margin="5"/>
        <Button Grid.Column="1" Grid.Row="2"  Name="buttonAdd" Content="Добавить дело" Click="AddCase" Background="#5EBEC4" Height="30" Width="100" Margin="5"/>
    </Grid>
</Window>
